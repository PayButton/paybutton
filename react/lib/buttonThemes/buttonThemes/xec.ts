import ButtonTheme from '../ButtonTheme';

const primary = '#0074C2';

const theme: ButtonTheme = {
  palette: {
    primary,
    secondary: '#f8fdf8',
    tertiary: '#231F20',
    logo: primary,
  },
};

export default theme;
