import Theme from '../Theme';

const primary = '#0074C2';

const theme: Theme = {
  palette: {
    primary,
    secondary: '#FFF',
    tertiary: '#231F20',
    logo: primary,
  },
};

export default theme;
