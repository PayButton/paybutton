import Theme from '../Theme';

const primary = '#4bc846';

const theme: Theme = {
  palette: {
    primary,
    secondary: '#f8fdf8',
    dark: '#374936',
    logo: primary,
  },
};

export default theme;
