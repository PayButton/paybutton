import Theme from '../Theme';

const primary = '#4bc846';

const theme: Theme = {
  palette: {
    primary,
    secondary: '#f7fdf7',
    dark: '#374936',
    logo: primary,
  },
};

export default theme;
