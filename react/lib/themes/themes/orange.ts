import Theme from '../Theme';

const primary = '#ee8b2b';

const theme: Theme = {
  palette: {
    primary,
    secondary: '#fefbf8',
    tertiary: '#504030',
    logo: primary,
  },
};

export default theme;
