import Theme from '../Theme';

const primary = '#0074C2';

const theme: Theme = {
  palette: {
    primary,
    secondary: '#f8fdf8',
    tertiary: '#231F20',
    logo: primary,
  },
};

export default theme;
