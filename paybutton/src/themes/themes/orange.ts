import Theme from '../Theme';

const primary = '#ee8b2b';

const theme: Theme = {
  palette: {
    primary,
    secondary: '#fefaf6',
    dark: '#504030',
    logo: primary,
  },
};

export default theme;
